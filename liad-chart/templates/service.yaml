apiVersion: v1
kind: Service
metadata:
  name: {{ include "liad-chart.fullname" . }}
  labels:
    {{- include "liad-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liad-chart.selectorLabels" . | nindent 4 }}
